# ── Secrets ───────────────────────────────────────────────────────────────
.env

# ── Python / build / cache ────────────────────────────────────────────────
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# ── Model weights (downloaded at runtime; live in the HF cache, not the repo)
*.bin
*.safetensors
models/

# ── Experiment outputs that are regenerated by running the pipeline ───────
results/raw/
results/scored*.json
*.log

# ── Intermediate DTG build artifact ───────────────────────────────────────
data/dtg_partial.json

# ── Private / internal docs — kept locally, never published ───────────────
DETAILED_SYSTEM_ROADMAP.md
RESEARCH_DESIGN_DOCUMENT.md
FINAL_RESEARCH_VERDICT.md
RESEARCH_ARTICLE.md
SOCIAL_POSTS.md

# ── OS / editor cruft ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
